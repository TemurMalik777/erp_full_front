
import type {GroupLessons} from "@types"
const GroupLesson = ({lessons}: GroupLessons) => {
    console.log(lessons);
  return (
    <div>
      <h1>GroupLesson</h1>
    </div>
  );
};

export default GroupLesson;
